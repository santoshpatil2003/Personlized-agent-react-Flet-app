export const TableRowMode = {
  DEFAULT: "TableCell--default",
  GMS_SCHEDULE: "TableCell--gms_schedule",
  GMS_TRANSACTION: "TableCell--gms_transaction"
};

export const TableRowModeValue = new Set(Object.values(TableRowMode));