import { IconShape, IconShapeValues } from "../icon/icon.constants";
import { has } from "../../util/has";

export const SELECT_REPORTS_STYLES = {
  triggerStyle: (item) => ({
    border: "none",
    background: {
      normal: item.mode === "primary" ? "#2356F6" : "#656677",
      hover: item.mode === "primary" ? "#193CAC" : "#767788",
      active: item.mode === "primary" ? "#193CAC" : "#767788",
    },
    text: {
      normal: "white",
      hover: "white",
      active: "white",
    },
    iconStart: {
      // show custom icon if it's a number, and it's a valid icon
      show: !Number.isNaN(item.icon) && has(IconShapeValues)(item.icon),
      iconProps: {
        shape: item.icon,
        color: "white",
      },
    },
    iconEnd: {
      show: item.favourite,
      iconProps: {
        shape: item.favourite ? IconShape.STAR_FULL : IconShape.STAR_EMPTY,
        color: "#57BFA1",
      },
    },
    arrowIcon: {
      show: true,
      color: {
        enabled: "white",
      },
    },
  }),
  itemStyle: (item) => ({
    background: {
      normal: "rgba(2, 0, 38, 0.05)",
      hover: "rgba(2, 0, 38, 0.15)",
      active: "rgba(2, 0, 38, 0.25)",
      selected: {
        normal: "rgba(2, 0, 38, 0.15)",
        hover: "rgba(2, 0, 38, 0.25)",
        active: "rgba(2, 0, 38, 0.35)",
      },
    },
    text: {
      normal: "black",
    },
    iconStart: {
      // show custom icon if it's a number, and it's a valid icon
      show: !Number.isNaN(item.icon) && has(IconShapeValues)(item.icon),
      iconProps: {
        shape: item.icon,
        color: item.mode === "primary" ? "#2356F6" : "#656677",
      },
    },
    iconEnd: {
      show: item.favourite,
      iconProps: {
        shape: item.favourite ? IconShape.STAR_FULL : IconShape.STAR_EMPTY,
        color: "#57BFA1",
      },
    },
    indicator:
      item.mode === "primary"
        ? "#2356F6"
        : item.mode === "secondary"
        ? "#656677"
        : undefined,
  }),
};
