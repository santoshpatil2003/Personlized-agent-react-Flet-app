export function currencyFormat(num, showTwoDigits) {
  if (num === void 0 || num === null) return 0;

  if (num % 1 > 0 || showTwoDigits) {
    return num.toLocaleString(undefined, {
      minimumFractionDigits: 2
    });
  }

  return num.toLocaleString();
}
