import { equals } from "./equals";

/**
 * Functional check to see if a list contains
 */
export const has = list => val =>
  list instanceof Set
    ? list.has(val)
    : Array.isArray(list)
    ? list.find(equals(val))
    : false;
