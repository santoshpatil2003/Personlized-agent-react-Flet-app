import type { BaseEmbedding } from "@llamaindex/core/embeddings";
import type { LLM } from "@llamaindex/core/llms";
import { PromptHelper } from "./PromptHelper.js";
import type { NodeParser } from "./nodeParsers/types.js";
/**
 * The ServiceContext is a collection of components that are used in different parts of the application.
 */
export interface ServiceContext {
    llm: LLM;
    promptHelper: PromptHelper;
    embedModel: BaseEmbedding;
    nodeParser: NodeParser;
}
export interface ServiceContextOptions {
    llm?: LLM;
    promptHelper?: PromptHelper;
    embedModel?: BaseEmbedding;
    nodeParser?: NodeParser;
    chunkSize?: number;
    chunkOverlap?: number;
}
export declare function serviceContextFromDefaults(options?: ServiceContextOptions): ServiceContext;
export declare function serviceContextFromServiceContext(serviceContext: ServiceContext, options: ServiceContextOptions): {
    llm: LLM;
    promptHelper: PromptHelper;
    embedModel: BaseEmbedding;
    nodeParser: NodeParser;
};
